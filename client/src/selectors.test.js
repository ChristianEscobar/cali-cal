/*
import * as selectors from './selectors';

describe('selectors' , () => {
  it('getEventCounts', () => {
    const someState = {
      days: {
        0: [ {}, {}, {}],
        1: [],
        2: [ {}, {} ],
        3: [],
      }
    };
    const result = selectors.getEventCounts(someState);
    expect(result).toEqual([3, 0, 2, 0])
  });
});
*/